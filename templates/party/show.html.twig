{% extends 'base.html.twig' %}

{% block title %}Party{% endblock %}

{% block body %}




<div class="container">

    <h1 class="partyTitle">{{ party.name }}</h1>

    <img class="img-fluid rounded mx-auto d-block" src="{{ asset('uploads/' ~ party.picture) }}" alt="{{ party.name }}">
    <div class="row">
<div class="col-md-4"></div>
    <div class="partyInfo col-md-4">
        <p>du : {{ party.startedAt|date('d-m ') }} au {{ party.endedAt|date('d-m-Y ') }}</p>

        <p class="partyCity">{{ party.city }}</p>
    </div>
    <div class="col-md-4"></div>



<div class="col-md-4"></div>
<div class="col-md-4"><p class="partyDescription">{{ party.description }}</p></div>
<div class="col-md-4"></div>

    </div>
</div>



    <h4>Trajet aller</h4>
    <div class="aller ">
        {% for aller in allers %}
            <a href="{{ path('travel_show', {'id':aller.id}) }}">
                <article class="col-md-3 cardaller" >
                    {% include 'travel/cardaller.html.twig' with {'travel' : aller} only %}
                </article>
            </a>
        {% endfor %}
    </div>
    <h4>Trajet retour</h4>
    <div class="retour ">
        {% for retour in retours %}
            <a href="{{ path('travel_show', {'id':retour.id}) }}">

                <article class="col-md-3 cardretour">
                    {% include 'travel/cardretour.html.twig'with {'travel' : retour} only %}
                </article>
            </a>
        {% endfor %}

    </div>

    <a class="btn btn-primary" role="button" href="{{ path('travel_new',{'id': party.id}) }}">Proposer un trajet</a>


    {#<table class="table">#}
        {#<tbody>#}
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ party.id }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Name</th>#}
                {#<td>{{ party.name }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Picture</th>#}
                {#<td>{{ party.picture }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>City</th>#}
                {#<td>{{ party.city }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Started_at</th>#}
                {#<td>{{ party.startedAt ? party.startedAt|date('Y-m-d H:i:s') : '' }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Ended_at</th>#}
                {#<td>{{ party.endedAt ? party.endedAt|date('Y-m-d H:i:s') : '' }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Description</th>#}
                {#<td>{{ party.description }}</td>#}
            {#</tr>#}
        {#</tbody>#}
    {#</table>#}

    {#<a href="{{ path('party_index') }}">back to list</a>#}

    {#<a href="{{ path('party_edit', {'id': party.id}) }}">edit</a>#}

    {#{{ include('party/_delete_form.html.twig') }}#}
{% endblock %}
