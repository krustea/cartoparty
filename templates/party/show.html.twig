{% extends 'base.html.twig' %}

{% block title %}Party{% endblock %}

{% block body %}
    <h1>Party</h1>



<div class="container">

    <h1>{{ party.name }}</h1>
    du : {{ party.startedAt|date('d-m ') }} au {{ party.endedAt|date('d-m-Y ') }}

    <img src="{{ asset('uploads/' ~ party.picture) }}" alt="{{ party.name }}">
    <p>{{ party.description }}</p>

</div>

{#
    <h4>Trajet aller</h4>
    <div class="aller ">
        {% for aller in allers %}
            <a href="{{ path('travel_show', {'id':aller.id}) }}">
                <article class="col-md-3 cardaller" >
                    {% include 'travel/cardaller.html.twig' with {'travel' : aller} only %}
                </article>
            </a>
        {% endfor %}
    </div>
    <h4>Trajet retour</h4>
    <div class="retour ">
        {% for retour in retours %}
            <a href="{{ path('travel_show', {'id':retour.id}) }}">

                <article class="col-md-3 cardretour">
                    {% include 'travel/cardretour.html.twig'with {'travel' : retour} only %}
                </article>
            </a>
        {% endfor %}
#}
{#{% for travel in travels %}#}
    {#<div>#}
        {#<p>#}
{#{{ travel.city }}#}
        {#</p>#}
    {#</div>#}
    {#{% endfor %}#}

    </div>




    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ party.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ party.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ party.picture }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ party.city }}</td>
            </tr>
            <tr>
                <th>Started_at</th>
                <td>{{ party.startedAt ? party.startedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Ended_at</th>
                <td>{{ party.endedAt ? party.endedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ party.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('party_index') }}">back to list</a>

    {#<a href="{{ path('party_edit', {'id': party.id}) }}">edit</a>#}

    {#{{ include('party/_delete_form.html.twig') }}#}
{% endblock %}
